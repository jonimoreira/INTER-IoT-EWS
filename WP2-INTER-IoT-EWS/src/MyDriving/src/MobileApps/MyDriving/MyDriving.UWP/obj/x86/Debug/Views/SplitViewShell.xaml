<Page
    x:Class="MyDriving.UWP.Views.SplitViewShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MyDriving.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:ConnectionId='1' x:Name="MainGrid" >
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="{StaticResource Background}" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid x:ConnectionId='2' Background="{StaticResource SplitViewColor}" x:Name="HamburgerGrid">
            <Button x:ConnectionId='15' x:Name="HamburgerButton" Height="48" Width="48" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Background="Transparent"                               VerticalAlignment="Center" 
                    Foreground="White" FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </Grid>
        <Grid x:ConnectionId='3' Grid.Column="1" Background="{StaticResource SplitViewColor}"  x:Name="TitleGrid">
            <TextBlock x:ConnectionId='14' x:Name="PageTitle" Margin="10,0,0,0" Foreground="White" 
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"/>
        </Grid>
        <SplitView x:ConnectionId='4' x:Name="MyDrivingSplitView" DisplayMode="Overlay"  IsPaneOpen="False" CompactPaneLength="48" OpenPaneLength="200"   Grid.Row="1" Grid.ColumnSpan="2" BorderThickness="0" >
            <SplitView.Pane>
                <StackPanel x:ConnectionId='5' Background="Black"  x:Name="SplitViewPanel">
                    <Button x:ConnectionId='6' x:Name="NewTripButton"                              Style="{StaticResource SplitViewButtonStyle}">
                        <controls:SplitViewButtonContent x:ConnectionId='13' x:Name="Current" />
                    </Button>

                    <Button x:ConnectionId='7' x:Name="TripsButton"                           Style="{StaticResource SplitViewButtonStyle}">
                        <controls:SplitViewButtonContent x:ConnectionId='12' x:Name="PastTrips" />
                    </Button>

                    <Button x:ConnectionId='8' x:Name="ProfileButton"                             Style="{StaticResource SplitViewButtonStyle}">
                        <controls:SplitViewButtonContent x:ConnectionId='11' x:Name="Profile" />
                    </Button>

                    <Button x:ConnectionId='9' x:Name="SettingsButton"                              Style="{StaticResource SplitViewButtonStyle}">
                        <controls:SplitViewButtonContent x:ConnectionId='10' x:Name="Settings" />
                    </Button>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>

