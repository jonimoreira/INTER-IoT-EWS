<Page x:ConnectionId='1'
    x:Class="MyDriving.UWP.Views.PastTripsMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyDriving.DataObjects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton x:ConnectionId='2' Icon="Refresh" Label="Refresh" x:Name="RefreshButton"                            />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{StaticResource MainBackgroundColor}">
        <ListView x:ConnectionId='3' x:Name="ListView"                                                                                                  >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,4,0,0"  />
                    <Setter Property="Background" Value="{StaticResource MainBackgroundColor2}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PastTripsBorderColor}" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0,0,0,0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate                        >
                    <Grid x:ConnectionId='4'                                HorizontalAlignment="Stretch">
                        <FlyoutBase.AttachedFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem x:ConnectionId='5' Text="Delete"
                                                                                       />
                            </MenuFlyout>
                        </FlyoutBase.AttachedFlyout>


                        <Grid  HorizontalAlignment="Stretch" Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Image x:ConnectionId='6' x:Name="Image"                                                                                              HorizontalAlignment="Stretch"  Stretch="UniformToFill" />
                            <TextBlock x:ConnectionId='7'                      Margin="12,5,0,0" HorizontalAlignment="Left"  Grid.Row="1"/>
                            <TextBlock x:ConnectionId='8'                         HorizontalAlignment="Left" Foreground="{StaticResource CalculationTextColor}" Margin="12,0,0,5" FontSize="13.333" Grid.Row="2"/>
                            <TextBlock x:ConnectionId='9'                               HorizontalAlignment="Right" Foreground="{StaticResource CalculationTextColor}" Margin="0,0,12,5" FontSize="13.333" Grid.Row="2"/>
                           
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

