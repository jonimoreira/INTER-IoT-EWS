<Page
    x:Class="MyDriving.UWP.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource MainBackgroundColor}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden" Margin="15,0,15,0">
            <StackPanel Margin="0,16,0,0">

                <TextBlock  Text="Unit Preferences"  Style="{StaticResource SettingsLabelStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                  
                    <RadioButton x:ConnectionId='5' Name="Kilometers"  IsChecked="{Binding MetricDistance, Mode=TwoWay}" Margin="10,5,0,0" GroupName="A"  BorderBrush="Gray" >
                    Kilometers
                </RadioButton>
                <RadioButton x:ConnectionId='6' Name="Miles" Margin="10,5,0,0" IsChecked="{Binding MetricDistance, Mode=TwoWay, Converter={StaticResource NotConverter}}" GroupName="A" Grid.Row="1">
                    Miles
                </RadioButton>

                <RadioButton x:ConnectionId='7' Name="Liters"  IsChecked="{Binding MetricUnits, Mode=TwoWay}" Margin="10,5,0,0" GroupName="B" Grid.Column="1">
                    Liters
                </RadioButton>
                    <RadioButton x:ConnectionId='8' Name="Gallons" Margin="10,5,0,0" IsChecked="{Binding MetricUnits, Mode=TwoWay, Converter={StaticResource NotConverter}}" GroupName="B" Grid.Column="1" Grid.Row="1">
                    Gallons
                </RadioButton>
                </Grid>

                <Border  Style="{StaticResource SettingsSeparatorLine}" />


                <TextBlock Text="About" Style="{StaticResource SettingsLabelStyle}"/>

                <Button  Content="Copyright 2016 Microsoft" Style="{StaticResource SettingsAboutButtonStyle}" />
                <Button x:ConnectionId='1' Name="TermsOfUseButton" Content="Terms of use"                                 Style="{StaticResource SettingsAboutButtonStyle}" />
                <Button x:ConnectionId='2' Name="PrivacyPolicyButton" Content="Privacy policy"                                     Style="{StaticResource SettingsAboutButtonStyle}" />
                <Button x:ConnectionId='3' Name="OpenSourceNoticeButton" Content="Open Source Notice"                                        Style="{StaticResource SettingsAboutButtonStyle}" />
                <Button x:ConnectionId='4' Name="OpenSourceGitHubButton" Content="Open Source on GitHub"                                        Style="{StaticResource SettingsAboutButtonStyle}" />

            </StackPanel>
        </ScrollViewer>
      
    </Grid>
</Page>

